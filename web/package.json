{
  "name": "so101-browser-sim",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based MuJoCo simulation of the SO-101 arm (Three.js + esbuild).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "npx serve dist"
  },
  "dependencies": {
    "@mujoco/mujoco": "3.13.0",
    "three": "0.186.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
